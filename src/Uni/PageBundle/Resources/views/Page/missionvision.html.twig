{% block missionvision %}
    <section id="missionvision" class="bg-gray-lighter">
        <div class="container">
            <div class="row text-center">
                <div class="col-lg-12">
                    <h2 class="section-heading">Nuestra Comunidad</h2>
                </div>
                <div class="col-lg-12">
                    <p class="text-muted">{% if frontpage %}{{ frontpage.frontpagemission|raw }}{% endif %}</p>
                    <p class="text-muted">{% if frontpage %}{{ frontpage.frontpagevision|raw }}{% endif %}</p>
                </div>
            </div>
        </div>
    </section>
{% endblock %}