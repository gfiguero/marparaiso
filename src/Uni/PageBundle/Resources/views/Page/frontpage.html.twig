{% block stylesheet %}
    {% set photo = random(frontpage.frontpagephotos) %}
    <style type="text/css">
    .header-image {
        background-image: linear-gradient(rgba(0,0,0,0.3),rgba(0,0,0,0.3)),url('{{ asset(photo.webpath) | imagine_filter('page_frontpage') }}');
    }
    </style>
{% endblock %}
